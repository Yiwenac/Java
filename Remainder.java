class Remainder{

  public static void main(String[] operands){
    System.out.println(Integer.parseInt(operands[0]) % Integer.parseInt(operands[1]));
    //Integer.parseInt() convert a string of digits to an Integer.
  }
}
